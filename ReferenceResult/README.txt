This folder holds input data for testing TauDEM.

Run the script testall.bat then compare the answers to results in ReferenceResult folder.

The command 
testall > testall.out 2>&1 
can be used to redirect all output to a single file. 